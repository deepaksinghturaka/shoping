<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
           .item {
            color: black;
            transition: all .3s linear;

            &:hover    {
            border-bottom: 2px solid red;
            border-radius: 3px;
            padding: 1px 5px;
            color: red;
            }
        }
    </style>
</head>


<body class="font-['helvetica_now_display']">
    <nav class="w-full flex justify-between  px-5 py-3">
        <h3 class="text-xl">Scatch</h3>  
     
        <% var loggedin = typeof loggedin !== "undifined" ? loggedin : true  %>
        <% if(loggedin){ %>
            <div class="flex gap-5">
                <a class="item" href="/shop">Shop</a>
                <a class="item" href="/cart">Cart</a>
                <a class="item" href="/account">My Account</a>
                <a class="text-red-600 item" href="/users/logout">Logout</a>
            </div>
       <% } %>
    </nav>

    
    